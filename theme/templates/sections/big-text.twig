{% extends 'sections/section.twig' %}

{% set section_classes = ['big-text', 'fit-height'] %}

{% block content %}

    <div class="columns">
        <div class="column has-text-centered">
            <h1>{{ text }}</h1>
        </div>
    </div>

{% endblock %}